import React from 'react';
import PropTypes from 'prop-types';
import styles from './Clients.module.scss';

const Clients = () => (
  <div className={styles.Clients} data-testid="Clients">
    Clients Component
  </div>
);

Clients.propTypes = {};

Clients.defaultProps = {};

export default Clients;
